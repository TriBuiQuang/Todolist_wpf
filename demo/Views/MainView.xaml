<Window x:Class="demo.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:demo.Views"
        mc:Ignorable="d"
        Title="Todo list" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <!--Column 1-->
        <!--x:Name="LoadPageDashBoard" is binding to ViewModels-->
        <StackPanel>
            <Button x:Name="LoadPageDashBoard">DashBoard</Button>
            <Button x:Name="LoadPageRegistration">Registration</Button>
            <Button x:Name="LoadPageLogin">Login</Button>
        </StackPanel>
       
        <!--Column 2-->
        <ContentControl Grid.Column="1"
                        x:Name="ActiveItem"/>
    </Grid>
</Window>
